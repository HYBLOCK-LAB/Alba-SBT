# Alba-SBT
26-1 DApp Course

## Backend

B-1/B-2 shared NestJS server scaffold lives in `backend`.

```bash
cd backend
npm install
cp .env.example .env
npm run start:dev
```

Fill `.env` with Supabase keys from PM and JWT settings from C before running against real data.
